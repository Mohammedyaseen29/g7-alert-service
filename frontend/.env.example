# Backend origin used by the browser at build time.
# Use your HTTPS API origin in production. If Nginx serves the frontend and
# proxies /api and /ws on monitoring.prideengs.com, use that same origin.
VITE_BACKEND_URL=https://monitoring.prideengs.com
